# ===== 不入库的文件 =====

# 源图片：index.html 已把图片转成 base64 内嵌，线上只需要 index.html。
# 原始 uploads/ 体积大且非部署必需，忽略它可避免仓库膨胀、也避免误传。
uploads/

# 本地工作区记忆（Daily Log / 缓存），纯本地用，不入库。
.workbuddy/

# 本地生成产物目录（若有），最终产物是仓库根 index.html。
outputs/
